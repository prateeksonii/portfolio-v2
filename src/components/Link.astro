---
export interface Props {
  label: string;
  href: string;
  path: string;
}

const { href, label, path } = Astro.props as Props;
const pathname = new URL(Astro.url).pathname;
---

<a href={href} class={pathname === path ? "text-blue-400 font-medium" : ""}
  >{label}
</a>
